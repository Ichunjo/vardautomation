version: 2

build:
  image: 'testing'
  apt_packages:
    - build-essential

sphinx:
    configuration: docs/conf.py

formats: all

python:
    version: '3.9'
    install:
    - requirements: docs/requirements.txt

